[gd_resource type="Resource" script_class="BuildingProfile" load_steps=2 format=3]
[ext_resource type="Script" path="res://scripts/data/building_profile.gd" id="1"]
[resource]
script = ExtResource("1")
id = &"graphite_mine"
display_name = "石墨矿场"
building_kind = "collector"
technology_tags = PackedStringArray("tech.geological_prospecting")
construction_days = 0
owner_profession_id = &"industrialist"
owner_slots_per_building = 1
employee_profession_ids = PackedStringArray("miner")
employee_slots_per_building = PackedInt64Array(10)
input_good_ids = PackedStringArray("electricity")
input_quantities_per_day = PackedInt64Array(150)
output_good_ids = PackedStringArray("graphite")
output_quantities_per_day = PackedInt64Array(2200)
resource_ids = PackedStringArray("natural_graphite")
resource_quantities_per_day = PackedInt64Array(2200)
resource_interaction_modes = PackedStringArray("extract")
resource_access_modes = PackedStringArray("local")
behavior_id = "consume_local_resources"
wage_policy_id = "adaptive"
wage_per_employee_per_day = 5000
