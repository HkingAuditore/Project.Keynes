# 2026-07-13

## 安装项目内所有 skill 到 WorkBuddy user-level
- 项目 skill 散落在 `.claude/`、` .codex/`、` .cursor/`、` .codebuddy/` 多工具目录，去重后 9 个独立 skill。
- 已安装、此前缺失的 3 个（安全审计均 P2 安全，纯文档 + 只读校验脚本）：
  - `project-keynes-country-runtime`（来源 `.codex/skills`）
  - `project-keynes-economy-runtime`（来源 `.codex/skills`）
  - `project-keynes-ui-art-direction`（来源 `.cursor/skills`）
- 已存在的 6 个中 5 个与项目副本逐字节一致；`project-keynes-dots-deepening` 仅路径前缀不同：
  - user-level 用 `docs/.../Project/.../gdext/...`（相对当前仓库根，本机正确布局）
  - `.cursor` 副本用 `Project.Keynes/docs/...`（假设外层多包一层 Project.Keynes）
  - 结论：user-level 版本更适配本机 checkout，**不要**用 `.cursor` 副本覆盖它。
- 审计注意：`verify_economy_runtime.ps1` 硬编码了 Godot 路径 `F:\Developent\Godot\...`（含拼写错误，本机不存在），仅 `-Godot` 时报错退出，无危害。
