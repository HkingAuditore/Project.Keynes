# 2026-07-09

## git 仓库更新（master）
- 状态：本地落后 origin/master 2 提交（60c3cb0, 2982ba6 "update"），且有 24 个未提交改动文件，与远程改动高度重叠。
- 对比：24 个文件的本地工作区内容哈希 == origin/master（同一份工作），零内容冲突。
- 障碍：Godot 编辑器（PID 75996, Godot_v4.6.2-stable_win64）锁定 `bin/windows/~dots_ext.windows.template_debug.x86_64.dll`（Godot 生成的 ~ 备份 dll，被误提交且被跟踪），导致 `git pull --ff-only` 报 `unable to unlink old ... Invalid argument`。
- 解决：① 用户关闭 Godot 释放锁；② `git stash` 收起 24 改动（安全网）；③ 验证 work==remote 后 `git reset --hard origin/master` 将 master 与工作区对齐到 60c3cb0；④ drop 临时 stash。
- 结果：master == origin/master == 60c3cb0，工作区干净，24 改动完整保留。
- 待办：远程的 `~dots_ext...dll` 误提交文件建议清理；本地 `tmp/` 下录制 csv 与 `git_compare` 报告为未跟踪，不提交。
