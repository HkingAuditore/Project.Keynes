[gd_resource type="Resource" script_class="BuildingProfile" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/data/building_profile.gd" id="1"]

[resource]
script = ExtResource("1")
id = &"textile_workshop"
display_name = "纺织工坊"
owner_profession_id = &"landlord"
owner_slots_per_building = 1
employee_profession_ids = PackedStringArray("worker")
employee_slots_per_building = PackedInt64Array(20)
input_good_ids = PackedStringArray("grain")
input_quantities_per_day = PackedInt64Array(2000)
output_good_ids = PackedStringArray("cloth")
output_quantities_per_day = PackedInt64Array(8000)
wage_policy_id = "fixed"
wage_per_employee_per_day = 5000
condition_opcodes = PackedInt32Array(1)
condition_signals = PackedInt32Array(8)
condition_compares = PackedInt32Array(0)
condition_reference_ids = PackedStringArray("")
condition_values = PackedInt64Array(0)
