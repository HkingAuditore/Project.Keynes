# 2026-07-08 工作记录

## C++ cell-range 重构具体方案（用户「做 1/2/3」→ 先出方案）
- 当读 world_ext_physical.cpp / world_ext.h / map_baker.gd 后两处修正（已写入 tmp/cutting_native_daily_ocean.md §7.7）：
  - SLP 的 Pass A(1782 `slp_passA_range`)/Pass B(1885) 已是 `pk::parallel_for_range` range-lambda（注释"逐 cell 独立、按 cell 区间并行、bit-equal"）→ 切片主要给已有 lambda 加 `[start,end)` 边界，非重写 kernel。
  - PSI 在 GDScript 已拆 INIT/ITERS/FINALIZE，ITERS 用 `_PER_STEP` 推进迭代（map_baker.gd:541/6894）→ 迭代已跨 tick 摊；剩每 call 仍跑全 water 域。
- **Item 1（砍每调用固定开销）是主线索、应最先做、零接口变更**：phys 尖峰 ~1.3ms 是固定成本（slot-id 解析 `component_id(StringName)` + is_water_lut 重建 + neighbor_indices marshaling），cell-range 削不动。PSI kernel 仅 0.4ms/UPWELLING 0.65ms → 砍掉固定开销后单 call 即 <1ms，无需 cell-range。
  - 改 world_ext.h（~1914 `_psi_*` 旁）加 `_phys_sid_*`/`_phys_is_water_lut`/`_phys_neighbor_indices` + 指纹失效；四个 leaf pass（slp 1506/wind 213/psi 2062/circulation 1333）把 `component_id(StringName)`/is_water_lut 栈重建/`knobs["neighbor_indices"]` 换成缓存成员，仿 PSI 指纹法(2228-2277)。
- **Item 2（cell-range）WIND/SLP 必需**：通用 clamp 模板仿光栅(866-872)；WIND coast BFS(338-394/396+) 抽出 `_phys_ensure_wind_coast` 成员缓存；SLP recenter/p95/融合/slop_out 发布(1907-1975) 用 `start_idx==0`/末切片 idiom 只跑一次；PSI finalize(2424+) 加 `end_k==n_water` 守卫；GDScript `_physical_solve_step_one` 加 `_phys_cell_cursor` 驱动 SLP/WIND/UPWELLING 切片。stage 名不变(phys_slp/wind/psi_init...)。
- **Item 3**：同 PR 更新 docs/cpp-dots-runtime/{computation-pipelines, scheduling-and-job-graph, performance-diagnostics-playbook}.md。
- ⛔ 本环境无法 rebuild DLL / headless PROBE，只出设计；落地须用户本地验收（bit-equal + fallback==0 + 每切片 p95/max<1ms，§7.7.4）。fallback 未达前不得 ACTIVE。

## 修复 Item 1/2 的 C++ 编译错误（用户 scons 报错）
`world_ext_physical.cpp` 三个真错误导致 scons 失败，已全部修复：
1. **成员名不匹配**（run_wind_field_pass 350-352）：cpp 误用 `_phys_wind_coast_sea_dist` / `_phys_wind_coast_sea_land_x/y`，header 实际声明为 `_phys_wind_sea_dist` / `_phys_wind_sea_land_x/y` → 已改对。
2. **COAST_INF 作用域外**（run_wind_field_pass 457/479 用，但仅 `_phys_ensure_wind_coast` 内局部 `constexpr` 声明）→ 在文件顶层（#include 后）加 `static constexpr int8_t COAST_INF = 127;`，两函数共享（局部声明仍保留、同值无害）。
3. **多余右花括号**：SLP 段 `_phys_slp_thermal_abs` 的 `if/else` 后误多一个 `}`，使 run_slp_field_pass 函数体在 1783 提前闭合 → 其后的 Latitude LUT / Pass A 等被解析到文件作用域，引发 knobs/_slp_insol_mean_lut/LUT_INSOL_MEAN/POSX 等一连串"未声明"级联错误 → 已删除该多余 `}`。
- 验证：四个函数（run_wind_field_pass 217-1251 / run_physical_circulation_pass 1252-1482 / _phys_ensure_wind_coast 1483-1601 / run_slp_field_pass 1603-2155）花括号净数均 0；cpp 内全部 34 个 `_phys_*` 标识符都能在 header 找到声明；无残留错误成员名。
- 结论：编译错误已全部消除（静态核对）。仍须用户本地 scons 真编译 + DLL 验收（bit-equal 等）。GDScript 早前缩进错误已修，本次未再报。

## 修复 GDScript 解析错误（用户 Godot 报大量 Parse Error）
C++ 编过后 Godot 报 `map_baker.gd:6554 Expected statement, found "Indent"`（其余 main/hex_renderer/map_generator 报错均为级联）。根因：Item 2b 在 `_PHYS_STAGE_SLP` case 内插入的切片块（6542-6552 行）整体**少了一层缩进（T4 而非 T5）**，把原本在 `if n_slp>0` 体内的 `var ret_slp`（HEAD 原在 T5）拖到 T4，使其后 `if ret_slp != null`（T5）成为"无块属主的子句"→ 报 Indent。已从 HEAD 取原始缩进核对：原 `var ret_slp`/`if ret_slp` 均在 T5。修复：把切片块 + `var ret_slp` 整体 +1 tab（6542-6553）→ `var ret_slp` 与 `if ret_slp` 现均 T5，与原始一致。
- 验证：对全文件做孤儿缩进扫描（排除 `%/#/运算符/+` 续行），编辑区（6500-7080）零孤儿；WIND（6648-6752）、UPWELLING（7040-7066）handler 缩进核对正确；line 6554 不再出现。用户已确认 Godot 解析通过、新 perf 数据已产（perf_record_20260708_150133.csv）。
- 注：Godot `--headless --quit` 不会真正解析 map_baker.gd（脚本懒加载），故纯靠缩进启发式+HEAD 对照验证，无法在此环境真跑 Godot。

## 新 perf 数据对比（用户修完解析错误后跑 perf_record_20260708_150133.csv）
同口径（perf_aggregate2.py 同脚本重跑两 CSV）：
- 旧基线 07-07（491 tick）/ 新 07-08（778 tick）：fps 59.49→59.76（≈持平）；over1ms 91.9%→92.0%；**fallback==0**（唯一值是 "false" 无回退哨兵，389/245 次）。→ Item 1/2 inert 验证安全、零回归。
- **主尖峰仍是 `gdext_native_daily_slice`（native_daily_sim 作业 ~51.5%）**：native_daily_complete 7.44 / weather 6.88 / climate_pass_a 6.26 / ocean_water 4.51 / sea_ice 4.17 / wind_air 3.36 ms —— **不在 Item 1/2 范围**（Item 1/2 只覆盖 ocean_currents 四 leaf pass）。
- ocean phys（Item 2 目标，gdext path）：phys_wind 2.98 / phys_slp 2.56 / phys_psi_init 2.84 ms，已 < 3ms budget，**非当前瓶颈**；开启 cell slicing 仅给移动端 <1ms 余量。
- 结论：fps 已锁 60；ocean_currents 已切片（slices mean 0.7）；native_daily_sim slices mean 0.9（几乎单切片 → 7.44ms 整段落一帧）。**真正 fps 杠杆 = native_daily_sim 节点级 cell-range**（cutting_native_daily_ocean.md §7），需另立项；Item 1/2 可 landing 但非瓶颈。
- 交付：perf_report_20260708.md（新）+ perf_report_20260707_v2.md（同口径基线）。聚合脚本 perf_aggregate2.py。
