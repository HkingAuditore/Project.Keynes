### 坑零：连续高度场做不出云海

**这是最根本的一条，排在所有光照问题之前。**

FBM 是一张**连续**的起伏面。从上方看，无论怎么调光、加多少散射项，它都是大理石纹
／流体涡纹／连绵丘陵。而真实云海从上方看是**一群互相独立的圆鼓包**，鼓包之间有
清晰的暗缝 —— 这个「离散性」才是云海的第一识别特征。

连续性是形状层面的错误，不可能靠着色补救。

#### 不要用 Worley F1（踩过，别再回去）

直觉上 Worley F1（到最近特征点的距离）正好是「鼓包 + 缝网」，但它有个硬伤：

```
F1 = min(各候选距离)
```

在「哪个特征点最近」发生切换的 locus 上，**梯度不连续**。于是每个胞的边界都是一道
折痕，整屏显出一张 Voronoi 多边形网：直线的硬棱 + 发光的胞心，像裂冰或彩绘玻璃。
拿它当法线比连续 FBM 更难看。

换光滑的鼓包剖面**救不了** —— 折痕来自 `min()` 本身，不在剖面里。

#### 正确做法：把核相加，而不是取距离的 min

每个特征点放一个光滑核然后**求和**（metaball）：

```
k(q) = (1-q)³,   q = |p-c|² / R²,   q ≥ 1 时为 0
```

在 `q=1` 处值与一阶导同时为 0，于是：

- 处处 C1 连续，**一道折痕都没有**；
- 每个特征点鼓起一个圆包，重叠处柔和融合 —— 正是积云顶部一朵一朵、又互相挤在
  一起的样子；
- 全程只用 `q = |v|²`，**不需要 sqrt**。

`R ≤ 1` 格时 3×3 邻域足够覆盖所有可能落在半径内的特征点。

`cloud_puff_field_d()` 在此之上分层：

| 层 | 权重 | 作用 |
|---|---|---|
| 鼓包层 0 | 1.00 | 主尺度的包，决定"一朵"有多大 |
| 鼓包层 1+ | 0.34 / 0.12 | 包上的次级小包 |
| billow FBM 尾巴 | 0.18 | 只啃边缘碎毛；给多了就退回大理石纹 |

**权重必须强烈偏向第一层。** 云海是「一个主尺度的包 + 表面细节」；各层等权会糊成
一片均匀泡沫，一朵一朵的形就没了。

#### 配套的三件事

- **哈希不能用 `fract(sin(·))`。** 那个哈希相关性很强：用在**梯度**噪声里误差会被
  插值掩盖，但鼓包场的哈希输出直接就是特征点**位置**，相关性会变成肉眼可见的成排
  鼓包和对角条纹。`sin()` 的精度还依赖 GPU 厂商。用 `cloud_hash_cell()`。
- **甲板要垫底噪（`deck_base`）。** 鼓包场只负责"高低"，但未探索区必须完全不透明；
  没有底噪，包间低谷会被 `density` 阈值判成透明，迷雾上出现一个个窟窿。
  垫底时梯度要同比乘 `(1 - deck_base)`。
- **`top_lo`/`top_hi` 窗口必须覆盖 `deck_base..1` 的实际分布。** 沿用 FBM 时期的窄
  窗口会让每个包心饱和成一块发光的白核。同理 `shadow_absorb` 也要下调 —— 鼓包场
  的高度差比 FBM 大得多，沿用旧值会把缝压成死黑。

### 坑零点五：域扭曲幅度必须远小于一个胞

一个噪声格约等于一个鼓包。域扭曲位移一旦超过一格，胞结构被彻底打散，得到的又是
流体涡纹。`warp_amount` 默认 0.22 格，只够把正圆揉成不规则形。

（早期版本用的是 1.75 格，这正是"完全看不出是云"的直接原因之一。）

