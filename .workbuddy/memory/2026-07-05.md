# 2026-07-05

## GDExtension 重建（re-run）

- 再次执行 `gdext/rebuild.sh`，构建成功（debug + release dylib 均更新，macos arm64，时间戳 02:41）
- 无编译报错，2026-07-01 修复的类型歧义问题仍生效
- dylib 路径：`Project/project-keynes/addons/dots_ext/bin/macos/`

## GDExtension 重建（第二次 re-run，14:05）

- 再次执行 `gdext/rebuild.sh`，scons 判定两个 dylib 均为 up to date，未重新编译/链接（源码自 02:41 以来无变化）
- 退出码 0，无报错
