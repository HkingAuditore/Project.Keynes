# 2026-07-17

- 分析 `tmp/economy_record_20260717_104150_v5_cell1110_q21_r18_*` 五份经济 CSV，并结合 `gdext/src/economy_runtime.cpp` 与 building/goods/consumption profiles 生成诊断报告：`tmp/economy_diagnosis_cell1110_20260717.html`，指标 JSON：`tmp/economy_diagnosis_cell1110_20260717_metrics.json`。结论：审计 population/money/goods 全零；选中格人口 255→130，最低满意度曾到 0，最终 protein 仍为 worst need；全局失业峰值 1222、最终 275，持续问题更像 owner jobs/建筑产能塌缩（owner jobs 311151→114066，loss_suspended groups 742）而非账本错误。

- 同一 dump 的**第二版可视化诊断报告**已生成：`tmp/economy_diagnosis.html`（含 5 张 matplotlib 图表，自包含 HTML）。新澄清与结论：
  - 口径：summary.csv=世界级聚合；cohorts/buildings/market/resources=cell 1110（~130-255人,10建筑,11成交货物）。
  - **"大量失业"前提不成立**：cell 失业 1→0，profession=31(unemployed)从未出现；世界失业率峰值仅 0.39%。真正问题是**人口腰斩**(cell 255→130, 世界 cohort 6002→3395) + 非工资 subsistence 多数被误读为失业。
  - **零资本形成**：pending_construction 全程=0，建设是外生/地图生成，非价格/利润信号触发 → 成品短缺(shortage=65535,价顶天花板)永久化。
  - **货币仅限金银**(good_profile.gd:37-38 monetary_issue_value>0 仅 gold/silver) → 财富极化 ~1.3万倍(merchant/miner≈1-1.2万 vs forager≈0.9 货币单位/人)。
  - **留存机制**(_owner_retained_outputs)使 subsistence 产出脱离市场，成品 offered_supply_ema=0。
  - 改进优先级：①价格→内生建设 ②解除货币仅限金银 ③留存上限 ④上游减产/加sink ⑤雇佣结构+上升通道 ⑥出生率挂钩产能 ⑦资源真实稀缺。
  - 复现环境：venv `C:/Users/hkinghuang/.workbuddy/binaries/python/envs/default`(Scripts/, 非 bin/)，已装 pandas+matplotlib；EDA 脚本 `tmp/eda.py`，报告生成 `tmp/gen_report.py`。

- 分析 `tmp/economy_record_20260717_114418_v5_cell1031_q3_r17_*` 五份经济 CSV，并生成交互式诊断报告：`tmp/economy_record_20260717_114418_v5_cell1031_q3_r17_analysis.html`，聚合 JSON：`tmp/economy_record_20260717_114418_v5_cell1031_q3_r17_analysis_summary.json`。关键结论：全局 population/money/goods 审计误差均为 0；summary 全局失业 day240 首现、day1930 达 332；选中 cell1031 本身无失业，但非商人人口 130→75，day105 满意度低于 50%，day175 首次人口下降。短缺核心是 processed_food/logs/game_meat/chipped_stone_tools/cloth/fur 长期 `shortage_q16=65535`，wild_game 资源 3349→252(-92.5%)，狩猎/火塘/石器链后期 capacity/output 归零。修正了 CSV 诊断 skill：market.good_id/resources.resource_id 可能是字符串 stable id，不能强转 int。

- **同一 cell1031 dump 的第三版深度诊断（含源码机制因果链）**：报告 `tmp/经济运行时深度诊断报告.html`（Chart.js 交互，dark 主题，回答用户 7 问）；数据 `tmp/_econ_data.json`；脚本 `_econ_probe/_econ_analyze/_econ_deep/_econ_causal/_econ_export/_build_report.py`。新增机制级根因（均带 economy_runtime.cpp 行号）：
  - **① 出生机制缺失（P0 总病根）**：`Signature::birth_rate_q32`、`MarketResult::births` 全 .cpp 从未写入，人口守恒式 births≡0（:9085）→ 人口只死不生，13.3万→5.9万单调崩。饥饿死亡 survival_q16<0.5 触发（:8603-8615）。
  - **② 产业链单点依赖雪崩（P0）**：chipped_stone_tools 唯一由 knapping_workshop(artisan) 产；artisan day15~215 最先破产停产 → 工具断供 → hunting_camp 失去硬投入 owner 48→0 → game_meat/fur 断供 → protein 需求崩。消耗型资源 wild_game 无 resource_generation 再生。
  - **③ 货币单向虹吸（P0）**：家庭消费+生产投入采购全额流入本地商人（:8484-8524），商人 25% 现金留存囤积，无回流通道；生产者财富占比 81.3%→0.1%，商人 13%→59%，精英 5.7%→40%。producer_support 仅 1/5 市价(DENOMINATOR=5)且随停产归零、靠铸币。
  - **④ 就业分配错配（P1）**：assign_building_employment(:4402-4730) 按"利润率↓→利用率↓"招人，同 signature 人口整体喂给高利润 group → 采集地 owner_required=7 却 filled=48、狩猎营地 required=48 却 filled=0。owner_required(诊断值,随利用率)与 filled(signature池实分)不同源。
  - **⑤ 物价只有加速器无刹车（P1）**：供给=0 时 excess+inventory+shortage 三重正压顶到 max_price 锁死；cost_anchor confidence=supply/demand 断供失锚；冻结期价格 ×epoch_days 放大。
  - id 语义：need/profession/building 均按 data/economy/* 文件名字母序 0-based（economy_catalog.gd:864 paths.sort）。need: 10=produce,11=protein,13=staple_food。建筑 type90=knapping_workshop,238=hunting_camp,69=gathering_ground。

- **cell1031 dump 第四版可视化报告（本轮交付）**：`tmp/经济运行时深度分析报告.html`（277KB，Chart.js dark 主题，8 KPI 卡 + 10+ 图，回答 7 问）；绘图数据固化 `tmp/econ_report_data.json`（252KB）。脚本 `tmp/econ_analysis_1~5.py`、`econ_build_report_data.py`、`econ_timeline.py`、`build_html.py`（venv 同上）。核心叙事："这是一个正在缓慢死亡的经济"——不是失业问题，是产业链断裂+财富抽血。关键数字：artisan 仅 3 人从 day1 结构性赤字(收2/支8)→工具断供→狩猎营地 output 455→0.4→猎人 48→0；精英存款占比 18.7%→99.9%；商人末期仅花采购预算 0.18% 囤积 367 万；石器工坊日产 4124 工具 vs 真实需求~120（过剩 30 倍）；wild_game -92.5% 不可持续；审计误差全 0 数据可信。

- **v7 复诊报告（本轮独立交付，对齐用户"这是修改后的新数据，请再次分析"）**：产物 `经济运行时v7复诊报告.html`（工程根目录，162KB，Chart.js dark 主题，v5→v7 对照 + 改善/未解决/改进建议）；数据 `tmp/_v7_data.json`（147KB）；脚本 `tmp/_v7_analyze/_deep/_export/_report.py`（venv 同上）。总体判定：**方向正确、部分见效，但出生与贸易两大新框架空转**。
  - **改善项（防榨干生效）**：生产者财富占比末期 0.1%→**61.8%**；worst-need 满意度 0.64(v5跌)→**0.90**(v7升，趋势逆转)；全局在岗末期 5.59万→**6.95万**。
  - **未解决（4 类）**：① births 仍≡0（字段 signature_birth_rate_q32 已就位:2332/2355，但 .tres 未配 birth_rate 默认 0 + MarketResult.births 无赋值 → 三重缺失），人口仍 133→78 与 v5 逐点相同；② **贸易系统空转**：trade_scan_total=288000 但 trade_completed_scans=0、candidates_generated=0、orders_arrived=0（只扫描不成交）；③ 产业链断裂原样复现（打石工坊/狩猎营地/织布棚停产，成品断供顶格 +698%~+835%）；④ loss_suspended_building_groups 0→2206；⑤ 新怪象 gathered_plants demand=23320/supply=156996 但 stock=0、shortage=16720（有供有需零成交）。
  - 改进优先级：**P0** 接通出生逻辑（.tres 配 birth_rate + MarketResult.births 赋值 + 守恒式生效）；**P0** 让贸易真正成交（candidate 生成→order 派发→arrive 闭环）；**P1** 打破产业链单点（chipped_stone_tools 多来源/降 hunting 硬依赖）；**P1** 排查 gathered_plants 有供有需零成交的撮合 bug。选中格(cell1060)人口轨迹与 v5(cell1031) 逐点相同（133→78，-41.4%；hunter/artisan 归零、forager/fisher 稳）；births 仍 0、pending_construction 仍 0、wild_game 仍 -90% 耗竭、加工品锁天花板价+shortage=1.0、初级品压地板价。v7 新增：① 世界级多格模拟 → 全局 loss_suspended 52→2206（崩塌在全球铺开 42×）；② 跨 cell 贸易脚手架（trade_runtime_mode=ACTIVE、topology=163、country=1983 就绪）但 orders_in_flight/dispatched/arrived/candidates_accepted 全 0 —— 未实际运转。精英存款占比终点 99.9%→38.2%、商人采购率 0.18%→10%，但属"建筑停摆抽干生产者存款基数"而非健康回流。源码印证：`economy_runtime.cpp:7782 _births=0` 且 `market_result.births` 无赋值处 → 出生机制仍缺失；git 未查（DLL 锁，未动仓库）。

- 恢复总结后的 v7 分析上下文并完成复诊输出：对比旧 `v5_cell1031` 与新 `v7_cell1060` 聚合 JSON，复核 `communal_hearth`/`stone_age_hunting_camp`/`knapping_workshop`/`gathering_ground` 配置和 `economy_runtime.cpp` 生产 capacity 逻辑。结论：v7 采集与生产者资金明显改善，但本地人口仍 133→78、失业峰值 46(day645)、processed_food/logs 全程短缺、gathered_plants 新增长期短缺；根因集中在 births 仍未接通、贸易/建设未闭环、火塘双输入 AND 导致 processed_food=0、狩猎链被工具+wild_game 双重卡死。最终呈现 `经济运行时v7复诊报告.html` 与 `tmp/economy_record_20260717_140702_v7_cell1060_q32_r17_analysis.html`。

- **第三份 v7 数据重分析（cell1166 q17,r19, day5→3705）三方对比**：脚本 `tmp/econ_compare_three.py`（v5 cell1031 / v7 cell1060 / v7 cell1166 三版统一计算）、`tmp/build_compare_three_report.py`；产物 `tmp/经济运行时_v5_v7cell1060_v7cell1166_三方对比.html`（849KB）、`tmp/econ_compare_three.json`。核心发现：**cell1166 进入近似稳态，不是崩得更慢**——wild_game 净增 +269%（12755→47082，前版均 -90% 耗竭）、满意度全程 1.0、总资金 14713→44316 单调增、猎人撑到 day2780 才归零、artisan 残存 2 人、人口 -37.7%（末 81）。这是资源闭环被修复（猎物再生）的证据。但**加工品缺货诅咒未破**：processed_food/tools/game_meat/logs/fur 仍锁天花板价+shortage=1.0；仅 **cloth 例外**（价 2.97→0.35、shortage=0，破缺货）。三版一致未变：births≡0、pending_construction≡0、跨 cell 贸易订单全 0、财富仍向精英集中。待核验：wild_game 再生是改了 resource_generation 还是种子差异 → 需 git diff（退出 Godot 释放 DLL 锁）。

- **cell1166 独立聚焦复诊报告（本轮交付，含源码断点核对）**：产物 `经济运行时v7第三次复诊报告.html`（工程根目录，197KB，Chart.js dark，6 图 + 三方对照表 + 链路 flow 图）；数据 `tmp/_v7b_data.json`；脚本 `tmp/_v7b_analyze/_deep/_export/_report.py`（venv 同上；pandas 3.0 需用 .ffill() 非 fillna(method=)）。这次 trade 诊断字段最全（trade_rejected_* 全套），得出**决定性判据**：
  - **贸易 bug 在 candidate 生成本身**：scan 累计 21.34 亿、source_signals=114、dest_signals=350、mode=ACTIVE、topology ready，但 candidates_generated=0、accepted=0、orders_arrived=0，**且所有 trade_rejected_*（profit/capacity/stock/cash/route/order_cap）累计全 0** → 若是"生成候选后被门槛拒"，rejected 会有数；rejected 全 0 说明候选压根没进评估环节。排查方向：candidate 生成函数提前 return 或 source×dest 配对逻辑没跑。per-good：119 good trade_enabled=1 但 import/export/inbound/outbound 全 0。
  - **出生源码三重缺失坐实**（economy_runtime.cpp）：`_births=0`(:7840)、`market_result.births` 唯一出现在 :9695 被**读取**累加、**全文件无写入点**；`sig_birth` 在 :2365 读入、:2388 存进 `_signatures[i].birth_rate`，但 signature→market_result.births 转换逻辑不存在。守恒式 :9378/:9695 已就绪。修法：市场结算处补 `births += _signatures[sig].birth_rate × population`(Q32 右移) + profession/ethnicity .tres 填 birth_rate。
  - **产业链拐点 day2405**：打制石器工坊(type90) 先停产 → 狩猎营地(type238) owner 48→0(day3605) → producer 126→77。（注意：此报告按 owner 归零判定拐点较晚，与三方对比报告"wild_game 再生 +269%"口径差异——本报告聚焦 owner/断供链，未展开资源再生对比。）

- 继续恢复并整理 `economy_record_20260717_152531_v7_cell1166_q17_r19_*` 新数据复诊，生成简明交付报告 `经济运行时v7_cell1166复诊报告.html`。结论保持：v7/cell1166 比 cell1060 明显改善（gathered_plants/fish/cloth 短缺为 0，本地失业峰值仅 1，wild_game +269%），但仍不健康：births 累计 0、贸易候选/订单/到达/容量全 0、processed_food/logs 全程短缺、hunter 48→1，石器/狩猎/火塘末期 capacity=0。优先修出生、贸易 candidate 生成、火塘双输入、狩猎工具硬依赖和 owner 承接。

- 基于同一 `v7_cell1166_q17_r19` 切片与两份既有 HTML 报告，整理经济系统稳健性/真实性优化方案：P0 为出生闭环、贸易 candidate 生成、石器时代 home_energy/logs 基础来源、火塘 OR/配方替代、狩猎工具依赖软化、owner retention/producer cashflow 保护；P1 为 perishables support/少丢弃、消费需求死亡阈值分层、就业 owner slot 防挤占、建设/贸易响应价格信号；P2 为新增 retained/support/quota/working-capital/trade-pair diagnostics 与 N=1 vs 5-day 对照验证。强调 C++ 仍为 cohort/market/building mutable owner，GDScript 只做 catalog/scheduler/query/I/O。

- 完成经济 runtime 贸易/采购/建设/清算闭环修复并通过 focused tests：`building_runtime_test.gd`、`economy_trade_runtime_test.gd`、`goods_storage_schema_test.gd` 全 PASS；Windows `template_debug` 与 `template_release` GDExtension 均构建成功。关键改动：`ensure_market_signal_index()` 懒创建 construction goods signal，施工材料 shortage 稳定进入 business demand/nonhousehold withdrawal/merchant target；trade relief pressure 支持 survival/input reserve zero-spread route；merchant target 响应 shortage；`cycle_flow` discard 前进入低价清算/support；修复硬输入极低产能下购买量截断为 0 导致免费生产的问题；文档同步 native economy / fixed-point ledger / domestic trade runtime。
